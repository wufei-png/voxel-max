# 像素小同事

- 内部 ID：`pixel-human`
- 主体状态：`release / first_pack_ready_for_submission`
- 主体性质：与真人无关的原创角色

身份方向已经锁定为“无脸冷面工作搭子”：面向现代工作聊天、略带技术气质，使用 `32×32` 原生身份网格、正面大头半身和受控前臂动作。A3 已被批准为 [`identity/masters/master-v1-32.png`](identity/masters/master-v1-32.png)，完整规则以 [`identity/spec.md`](identity/spec.md) 为准。

[`references/original-avatar.png`](references/original-avatar.png) 只保留为概念来源，候选目录只用于评审追溯；后续身份生产输入必须使用批准母版 v1。用户已确认原始参考的重绘、改编、公开发布与商用权利完整。

## 当前发布

1. 公开角色名为“像素小同事”，首套专辑名为“《小同事上班记》”。
2. 九张首发动作已锁定：`S01 收到` 使用 `v05-chat-card`，`S04 开会中` 使用 v5，`S09 磕头` 使用 v6 块状手掌优化版，其余沿用通过的当前版本。
3. [`packs/work-reactions/releases/v1/`](packs/work-reactions/releases/v1/) 已用最终动作重新生成平台中立源包、微信 GIF、横幅、封面与聊天图标，并通过发布 QA；投稿方式为免费且不开通赞赏。
4. `2026-07-16` 已用批准的 `S09 磕头` 资产完成通用 `$animated-sticker-maker` v0.8 canary；母包、独立渲染轨与微信派生物的两类 validation 均通过并绑定当前指纹。首次 `S08 下班了` 候选因文字碰边被正确拒绝，未改变首发发布包。

## 边界

- 像素小人不是方芯的新皮肤，也不沿用方芯的缺角、状态核心、无手脚或 S01-S08 动作规则。
- 新表情包从自己的 `S01` 开始编号，不接续方芯编号。
- 研究材料和候选图没有决策权；批准母版 v1 是当前唯一身份基准。
- 三张动态试片已通过；首包生产必须遵守当前包 README 与动作卡，不得重新采用研究报告的旧清单。
- 当前参考与真人无关，旧文件名中的个人标识不构成角色名称或身份来源。
- 两份研究报告的冲突已经在 [`identity/spec.md`](identity/spec.md) 中收敛；不再并行制作 A/B 两套完整方向。
- 身份试片门槛已经通过。当前包有意重新采用 v3 侧趴动作作为“崩了”，并把 v4 正面趴下动作改造成“磕头”；这属于用户确认的语义重分配，不是历史资产误混。
